import { atom } from 'recoil';

export const modelDrawerDisplayState = atom({
  key: 'modelDrawerDisplayState',
  default: false,
})
export const modelState = atom({
  key: 'modelState',
  default: null,
});